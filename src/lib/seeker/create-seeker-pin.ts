/**
 * STEP 7 — minimal seeker pin insert (re-export for a stable import path).
 * @see createSeekerPin in `@/services/seeker.service`
 */
export {
  createSeekerPin,
  type CreateSeekerPinPayload,
} from "@/services/seeker.service";
