/** Stable short hash for anonymous device id (server + client naming). */
export function hashDeviceId(s: string): string {
  let h = 0;
  for (let i = 0; i < s.length; i++) h = (h * 31 + s.charCodeAt(i)) >>> 0;
  return h.toString(16);
}
