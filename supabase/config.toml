# Supabase CLI: set project_id via `supabase link` or paste from the dashboard.
# Edge Functions were removed — this app uses Next.js API routes + Postgres migrations only.

project_id = "your-project-ref"

[api]
enabled = true
