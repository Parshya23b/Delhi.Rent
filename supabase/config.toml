# Supabase CLI — set project_id via `supabase link`.
# Geo platform (026): optional Edge job for match emails (mock logger).

project_id = "your-project-ref"

[api]
enabled = true

[functions.send-match-notifications]
verify_jwt = false
